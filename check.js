const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function run() { const [jc, cbt, j, ms, lp, st, mt] = await Promise.all([prisma.journalLine.count(), prisma.cashBankTransaction.count(), prisma.journalEntry.count(), prisma.savingsTransaction.count(), prisma.loanPayment.count(), prisma.storeSale.count(), prisma.unitTransaction.count()]); console.log(JSON.stringify({journalLines:jc, cashBankTx:cbt, journals:j, savingsTx:ms, loanPayments:lp, storeSales:st, unitTx:mt})); await prisma.$disconnect(); } run();
